<Window x:Class="PLAI.DownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Downloading"
        Width="560" Height="260"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        ShowInTaskbar="True">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="12"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="HeaderText"
                   Grid.Row="0"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Text="Downloading"/>

        <ProgressBar Grid.Row="2"
                     Height="18"
	                     IsIndeterminate="{Binding IsDownloadProgressIndeterminate, Mode=OneWay}"
                     Minimum="0" Maximum="100"
	                     Value="{Binding DownloadProgressPercent, Mode=OneWay}"/>

        <TextBlock Grid.Row="4"
                   FontSize="12"
                   Foreground="#666"
                   Text="{Binding DownloadProgressPercent, StringFormat={}{0:0}%}"/>

        <TextBlock Grid.Row="6"
                   FontSize="12"
                   TextWrapping="Wrap"
                   Foreground="#666"
                   Text="{Binding DownloadDetail}"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="96" Height="32" Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
