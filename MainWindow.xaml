<Window x:Class="PLAI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PLAI.ViewModels"
        xmlns:models="clr-namespace:PLAI.Models"
        mc:Ignorable="d"
        Title="PLAI" Height="720" Width="1100" MinHeight="520" MinWidth="820">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header / status -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="PLAI" FontSize="22" FontWeight="SemiBold"/>

            <TextBlock Text="{Binding DetectedHardwareSummary}" Margin="0,4,0,0" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding SelectionReason}" Margin="0,2,0,0" TextWrapping="Wrap"/>
            <TextBlock Text="{Binding StatusText}" Margin="0,6,0,0" FontStyle="Italic" TextWrapping="Wrap"/>

            <Expander Header="Model info" IsExpanded="False" Margin="0,10,0,0">
                <Border Padding="8" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="4">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0" Text="Id:" Margin="0,0,8,4"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedModel.Id}" TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" Text="Name:" Margin="0,0,8,4"/>
                        <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedModel.Name}" TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="2" Grid.Column="0" Text="Compute:" Margin="0,0,8,4"/>
                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedModel.ComputeTarget}" TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="3" Grid.Column="0" Text="Quant:" Margin="0,0,8,4"/>
                        <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedModel.Quantization}" TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="4" Grid.Column="0" Text="Min RAM:" Margin="0,0,8,4"/>
                        <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedModel.MinRamGb}" TextWrapping="Wrap"/>

                        <TextBlock Grid.Row="5" Grid.Column="0" Text="Min VRAM:" Margin="0,0,8,0"/>
                        <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SelectedModel.MinVramGb}" TextWrapping="Wrap"/>
                    </Grid>
                </Border>
            </Expander>
        </StackPanel>

        <!-- Startup download progress -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10" Visibility="{Binding IsStartupBusy, Converter={StaticResource BoolToVis}}">
            <TextBlock Text="{Binding DownloadDetail}" Margin="0,0,0,6" TextWrapping="Wrap"/>
            <ProgressBar Height="16"
                         Minimum="0"
                         Maximum="100"
                         Value="{Binding DownloadProgressPercent, Mode=OneWay}"
                         IsIndeterminate="{Binding IsDownloadProgressIndeterminate, Mode=OneWay}"/>
        </StackPanel>

        <!-- Chat transcript -->
        <ListBox Grid.Row="2" ItemsSource="{Binding Messages}" Margin="0,0,0,10">
            <!-- Make transcript feel like a log, not a selectable list -->
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Focusable" Value="False"/>
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="Transparent"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type models:ChatMessage}">
                    <!-- No borders around messages (per UX requirement) -->
                    <StackPanel Margin="0,0,0,10">
                        <TextBlock Text="{Binding Role}" FontWeight="SemiBold" Margin="0,0,0,2"/>
                        <TextBlock Text="{Binding Content}" TextWrapping="Wrap"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Input (Enter sends; no Send button) -->
        <DockPanel Grid.Row="3" LastChildFill="True">
            <Button DockPanel.Dock="Right"
                    Content="Cancel"
                    Width="100"
                    Height="32"
                    Margin="10,0,0,0"
                    Click="Cancel_Click"
                    Visibility="{Binding IsCancelVisible, Converter={StaticResource BoolToVis}}"/>

            <TextBox x:Name="InputTextBox"
                     Text="{Binding UserInputText, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     Height="80"
                     IsEnabled="{Binding IsInputEnabled}"
                     KeyDown="InputTextBox_KeyDown"/>
        </DockPanel>
    </Grid>
</Window>
